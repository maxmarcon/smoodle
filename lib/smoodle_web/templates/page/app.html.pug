div#app
	nav.navbar.navbar-expand-lg.navbar-light.bg-light
  		a.navbar-brand(href="#") Navbar
		div#navbarNavAltMarkup.collapse.navbar-collapse
    		div.navbar-nav
    			router-link.nav-item.nav-link(to="#{page_path @conn, :new_event, step: 1}" active-class="active") Event
    			router-link.nav-item.nav-link(to="#{page_path @conn, :poll}" active-class="active") Poll


	.container-fluid
		transition(name="slide" mode="out-in")
	 		router-view

